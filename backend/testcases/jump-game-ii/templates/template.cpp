#include <bits/stdc++.h>

using namespace std;

int solve(vector<int> &nums) {}

int main() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (int i = 0; i < n; i++) {
    cin >> a[i];
  }
  auto res = solve(a);
  cout << res << '\n';
}